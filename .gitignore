# Python environment (reproducible via requirements.txt)
venv/
__pycache__/
*.pyc

# Large / restricted raw datasets - get separately (see README), not pushed here
unified_oai_dataset.csv
dataset/
segmentation_data/

# Regenerated locally, not needed in git
clone_log.txt

# Local prediction test outputs
outputs/predictions/
